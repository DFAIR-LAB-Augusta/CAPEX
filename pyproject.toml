[project]
name = "capex"
version = "0.1.0"
description = "CAPEX, network attack capture for network security analysis"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.8",
]
